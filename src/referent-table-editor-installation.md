# テキストエディタ導入節の指示対象対応表

| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 候補語 | 初出定義 |
|---|---|---|---|---|---|---|
| ユーザー指示「『手元環境への導入』で今後のテキスト編集に用いるnanoのインストールセクションを設ける」、訂正「Windowsへのnanoインストールはwingetを既定の方法とする」および指定「GNU.nano」 | 後続の演習でファイルを編集する前に、利用者が実行できるエディタを準備する | 「手元環境への導入」の中で、Nano の有無を確認し、Windowsでは `winget install GNU.Nano` を既定の導入手段として、macOSとDebian / Ubuntuでは各OSのパッケージ管理コマンドで導入し、起動できることを確認する手順 | 開始条件 | 「手元環境への導入」の後続で行うテキスト編集より前 | テキストエディタの事前準備 | テキストエディタの事前準備とは、後続の演習へ進む前に、Nano の有無を確認し、Windowsでは `winget install GNU.Nano` を既定の手段として導入し、起動できる状態にする手順を指す。 |
| ユーザー指示「この中に vim/emacs/code(VSCode)/edit(for Windows) の選択肢も提示しておく」 | 利用環境や操作経験に合うエディタを一つ選べるようにする | 後続のファイル編集で、Nano、Vim、ターミナル内の Emacs、CLIから起動する VS Code、Windows 11 の Edit のいずれか一つを使う選択肢 | 手段 | Nano の導入手順と同じ節で提示し、後続のテキスト編集では選んだ一つを使う | 利用するテキストエディタ | 利用するテキストエディタとは、後続のファイル編集で使う Nano、Vim、ターミナル内の Emacs、CLIから起動する VS Code、Windows 11 の Edit のうち、利用者が選んだ一つを指す。 |
| ユーザー指示「git editorの既定をnanoにするconfigも実行」 | Gitが対話的な編集に使うエディターを、導入済みのNanoに統一する | `git config --global core.editor nano` を実行して設定するグローバルGit設定 | 手段 | Nanoの導入後に続く「Gitの初期設定」で、名前、メールアドレス、既定ブランチと合わせて実行する | Gitの既定エディター設定 | Gitの既定エディター設定とは、Gitがコミットメッセージなどの対話的な編集に使うコマンドを、グローバル環境でNanoに指定する設定を指す。 |
| Issue #2「Git初期設定の各コマンドに説明コメントを付ける」 | 初心者が実行前に各グローバル設定の設定対象と効果を区別できるようにする | 名前、メールアドレス、初期ブランチ名、Gitが対話的な編集に使うエディターを設定する4コマンドそれぞれの直前に置く `#` から始まる説明 | 記録 | 「Gitの初期設定」の各コマンドより前に置き、コメントを読んでから対応するコマンドを実行する | 設定内容コメント | 設定内容コメントとは、直後のコマンドが何を設定し、その値がどこで使われるかを実行前に示す説明を指す。 |
| ユーザー追加指示「またeditorの選択肢に応じてnano以外の設定方法にも触れる」 | 利用者が事前に選んだエディターをGitの対話的な編集にも使えるようにする | Nano、Vim、ターミナル内のEmacs、CLIから起動するVS Code、Windows 11のEditのうち、選んだ一つに対応する `core.editor` 設定コマンド | 手段 | エディターの選択後、「Gitの初期設定」でNanoを既定例として示し、その直後に選択肢別の設定例を示す | エディター別の設定例 | エディター別の設定例とは、事前に選んだエディターをGitの対話的な編集に使うため、選択肢ごとに対応する `core.editor` のコマンドを示したものを指す。 |
